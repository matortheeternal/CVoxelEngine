#include "ObjectParams.h"

ObjectParams::ObjectParams(int size, int xOffset, int yOffset, int zOffset) {
	this->size = size;
	this->xOffset = xOffset;
	this->yOffset = yOffset;
	this->zOffset = zOffset;
}